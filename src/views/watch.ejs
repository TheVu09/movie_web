<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Watch <%= movie.title %> - Movie Web</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        .video-container {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
        }

        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }
    </style>
</head>

<body class="bg-dark">
    <%- include('partials/navbar') %>

        <div class="container my-4">
            <div class="row">
                <div class="col-lg-10 mx-auto">
                    <h1 class="display-5 text-white mb-2">
                        <i class="bi bi-play-circle-fill text-danger"></i>
                        <%= movie.title %>
                    </h1>

                    <p class="text-white-50 mb-4">
                        <%= movie.overview %>
                    </p>

                    <% if (movie.trailerUrl) { %>
                        <div class="video-container mb-3">
                            <iframe src="<%= movie.trailerUrl %>" allowfullscreen
                                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture">
                            </iframe>
                        </div>

                        <% } else { %>
                            <div class="alert alert-danger d-flex align-items-center" role="alert">
                                <i class="bi bi-camera-video-off-fill me-2"></i>
                                <div>No video available for this movie.</div>
                            </div>
                            <% } %>

                                <div class="mt-4">
                                    <a href="/movie/<%= movie.id %>" class="btn btn-outline-light btn-lg">
                                        <i class="bi bi-arrow-left"></i> Back to Detail
                                    </a>
                                </div>
                </div>
            </div>
        </div>

        <%- include('partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>